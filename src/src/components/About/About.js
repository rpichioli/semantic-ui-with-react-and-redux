import React from 'react'
import PropTypes from 'prop-types'

class About extends React.Component {
	render () {
		return (
			<div>
				<p>semantic-ui-with-react-and-redux</p>
				<p>In development stage - SPA (Single-Page Application) show integration between React and Semantic UI in a React + Redux build.</p>
			</div>
		)
	}
}

export default About;
